
import './index.css';

/*
const Spoiler = require('./spoiler');


document.addEventListener('DOMContentLoaded', () => {

  const collapsibleElements = document.getElementsByClassName('collapsible');

  Array.prototype.map.call(collapsibleElements, element => new Spoiler(element, {
    maxLength: 300
  }));

});
*/
